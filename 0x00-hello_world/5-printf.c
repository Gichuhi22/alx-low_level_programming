#include <stdio.h>
/**
 * programs to output a string of characters
 * use the inbuilt function printf
 * main: main program entry
 * Return: always returns 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}
