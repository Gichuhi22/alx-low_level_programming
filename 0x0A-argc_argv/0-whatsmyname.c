#include "main.h"
/**
 * main - prints its name folled by new line
 * @argc: store the number of arguments passed to
 * the main function
 * @argv: a string array of all passed arguments to main
 *
 * Return: returns 0 (success)
 */
int main(int argc, char *argv[])
{

	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
