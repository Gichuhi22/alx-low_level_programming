#include "main.h"
#include <string.h>
/**
 *_strlen - returns the length of a string
 *@s: pointer to the string
 *
 *Return: returns an integer value
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
